info:
  name: getBlogs
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3003/api/blogs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
